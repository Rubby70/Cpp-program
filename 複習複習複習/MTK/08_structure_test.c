#include "stdio.h"

struct test {
    int qaq;
    // long long QQ[1000];
    // char arr[10];
    char c;
};

int main(void) {
    // 1. 這個struct的大小是多少？
    // 2. 在不使用 sizeof 的情況下，如何取得 struct 的大小
    return 0;
}
